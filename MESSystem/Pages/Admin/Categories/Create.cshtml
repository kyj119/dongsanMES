@page
@model MESSystem.Pages.Admin.Categories.CreateModel
@{
    ViewData["Title"] = "새 분류 등록";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0"><i class="bi bi-plus-circle"></i> 새 분류 등록</h4>
                </div>
                <div class="card-body">
                    <form method="post">
                        <div class="mb-3">
                            <label asp-for="Input.Name" class="form-label">
                                분류명 <span class="text-danger">*</span>
                            </label>
                            <input asp-for="Input.Name" class="form-control" placeholder="예: 태극기, 현수막, 간판" />
                            <span asp-validation-for="Input.Name" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Input.CardOrder" class="form-label">
                                카드 순서 <span class="text-danger">*</span>
                            </label>
                            <input asp-for="Input.CardOrder" type="number" class="form-control" min="1" />
                            <div class="form-text">
                                카드가 생성되는 순서입니다. (예: 1=태극기, 2=현수막, 3=간판)
                            </div>
                            <span asp-validation-for="Input.CardOrder" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <div class="form-check">
                                <input asp-for="Input.IsActive" class="form-check-input" type="checkbox" checked />
                                <label asp-for="Input.IsActive" class="form-check-label">
                                    활성 상태
                                </label>
                            </div>
                            <div class="form-text">
                                비활성화하면 주문서 작성 시 이 분류가 표시되지 않습니다.
                            </div>
                        </div>

                        <div class="alert alert-info">
                            <i class="bi bi-info-circle"></i> 
                            <strong>참고:</strong>
                            <ul class="mb-0 mt-2">
                                <li>카드 순서가 중복되면 같은 순서로 표시됩니다.</li>
                                <li>기존 분류: 1=태극기, 2=현수막, 3=간판</li>
                                <li>새 분류는 4번부터 시작하는 것을 권장합니다.</li>
                            </ul>
                        </div>

                        <div class="d-flex justify-content-between">
                            <a asp-page="Index" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> 취소
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-save"></i> 저장
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            @if (!ViewData.ModelState.IsValid)
            {
                <div class="alert alert-danger mt-3">
                    <i class="bi bi-exclamation-triangle"></i> 
                    입력 내용을 확인해주세요.
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
